# MCP Server Configuration
# This file shows the environment variables that can be configured

# Optional: Set logging level
# LOG_LEVEL=info

# Optional: Server name override
# SERVER_NAME=calculator

# Note: MCP servers communicate via stdio and don't require API keys
# The client (Claude Desktop, etc.) handles the AI model integration
